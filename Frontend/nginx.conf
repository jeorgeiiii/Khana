@"
server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files `$uri `$uri/ /index.html;
    }
}
"@ | Out-File -FilePath nginx.conf -Encoding ascii